import { Typography, Box } from '@mui/material';

const MonthlyAnalysis: React.FC = () => {
  return (
    <Box sx={{ padding: 2 }}>
      <Typography variant="h4">Monthly Analysis Report</Typography>
      <Typography>Monthly Analysis Report will go here.</Typography>
    </Box>
  );
};

export default MonthlyAnalysis;
