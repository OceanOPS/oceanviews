// src/components/PlatformTable.tsx
import { Typography, Box } from '@mui/material';

const Instrumentation: React.FC = () => {
  return (
    <Box sx={{ padding: 2 }}>
      <Typography variant="h4">Instrumentation</Typography>
      <Typography>Instrumentation dashboard will go here.</Typography>
    </Box>
  );
};

export default Instrumentation;
