// src/components/PlatformTable.tsx
import { Typography, Box } from '@mui/material';

const InteractiveMap: React.FC = () => {
  return (
    <Box sx={{ padding: 2 }}>
      <Typography variant="h4">Interactive map</Typography>
      <Typography>Interactive map dashboard will go here.</Typography>
    </Box>
  );
};

export default InteractiveMap;
