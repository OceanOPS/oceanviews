import { Typography, Box } from '@mui/material';

const CreateDashboard: React.FC = () => {
  return (
    <Box sx={{ padding: 13 }}>
      <Typography variant="h4">Create Dashboard</Typography>
      <Typography>Dashboard editor will go here.</Typography>
    </Box>
  );
};

export default CreateDashboard;
